[project]
name = "homeassistant-components"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "homeassistant>=2025.4.4",
]

[dependency-groups]
dev = [
    "pytest>=9.0.2",
]
